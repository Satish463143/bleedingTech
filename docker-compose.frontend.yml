services:
  frontend-next:
    build:
      context: .
      dockerfile: Dockerfile.frontend-next
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
